<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/membersGroupMenuButton"
        android:title="@string/title_members"
        android:color="@android:color/white"/>

    <item
        android:id="@+id/editGroupMenuButton"
        android:title="@string/title_edit_group"
        android:color="@android:color/white"/>

    <item
        android:id="@+id/exitGroupMenuButton"
        android:title="@string/title_exit_group"
        android:color="@android:color/white"/>

    <item
        android:id="@+id/advancedGroupMenuButton"
        android:title="@string/title_advanced"
        android:color="@android:color/white"/>
</menu>